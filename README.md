

## icpcoll

```text
Usage of ./icpcoll:
  -a string
        api接口来源 (default "miit")
  -f string
        单位名称文件
  -n string
        域名或单位名称
  -s string
        1,6,7,8 网站，app，小程序，快应用 (default "1")
```
